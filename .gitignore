# Android / Gradle
.gradle/
.kotlin/
build/
app/build/
captures/

# Local machine config
local.properties

# IDE files
.idea/
*.iml

# Native build cache
.externalNativeBuild/
.cxx/

# 签名密钥库与发布产物：含敏感凭据，泄露可被冒名签发假冒应用，严禁提交
release/
*.jks
*.keystore
# 本地签名凭据（storePassword/keyPassword 等），严禁提交；见 keystore.properties.template
keystore.properties

# Robolectric 单测运行时在模块根目录落地的 Room 数据库文件（运行态，非源码）：
# 曾被误提交为 v2 schema，导致后续版本升级后单测在此陈旧库上跑迁移而失败。严禁提交。
app/streak.db
app/streak.db-journal
