<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <View
        android:id="@+id/viewSheetDot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/bg_status_done" />

    <TextView
        android:id="@+id/tvSheetHabitTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="@string/habit_title_placeholder"
        android:textColor="@color/streak_ink"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/tvSheetHabitStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sheet_habit_status_done"
        android:textColor="@color/streak_accent"
        android:textSize="13sp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSheetToggle"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_marginStart="8dp"
        android:minWidth="0dp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="@string/sheet_habit_makeup"
        android:textSize="12sp"
        android:visibility="gone" />
</LinearLayout>
